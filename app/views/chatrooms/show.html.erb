<div class="row">
	<%=content_tag :div, '', class: "temp-info", data: { temp: current_user.id } %>
	<div class="col-md-8 col-md-offset-2">
	  <h1><%= @chatroom.topic %></h1>
		<div class="panel panel-default">
		  <p style="text-align: center;">Last 25 messages displayed only</p>
		  <% if @chatroom.messages.any? %>
		    <div class="panel-body" id="messages">
		      <%= render @chatroom.messages.for_display%>
		    </div>
		  <%else%>
		    <div class="panel-body hidden" id="messages">
		    </div>
		  <%end%>
		</div>

	  <%= render partial: 'messages/message_form'%>
	</div>
</div>